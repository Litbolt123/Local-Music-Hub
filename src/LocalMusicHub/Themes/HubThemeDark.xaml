<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Near-black Spotify-like shell; accent overridden at runtime (default purple) -->
    <Color x:Key="HubBgColor">#121212</Color>
    <Color x:Key="HubCardColor">#181818</Color>
    <Color x:Key="HubBorderColor">#2A2A2A</Color>
    <Color x:Key="HubTextPrimaryColor">#FFFFFF</Color>
    <Color x:Key="HubTextMutedColor">#B3B3B3</Color>
    <Color x:Key="HubAccentColor">#7C4DFF</Color>
    <Color x:Key="HubAccentHoverColor">#9B6BFF</Color>
    <Color x:Key="HubPrimaryForegroundColor">#FFFFFF</Color>
    <Color x:Key="HubMetricBgColor">#242424</Color>
    <Color x:Key="HubInputBgColor">#2A2A2A</Color>
    <Color x:Key="HubNowPlayingColor">#181818</Color>
    <Color x:Key="HubHoverColor">#2A2A2A</Color>
    <Color x:Key="HubSelectedColor">#333333</Color>
    <Color x:Key="HubSliderTrackColor">#4D4D4D</Color>
    <Color x:Key="HubSliderFillColor">#FFFFFF</Color>
    <Color x:Key="HubSliderThumbColor">#FFFFFF</Color>

    <SolidColorBrush x:Key="HubBgBrush" Color="{StaticResource HubBgColor}"/>
    <SolidColorBrush x:Key="HubCardBrush" Color="{StaticResource HubCardColor}"/>
    <SolidColorBrush x:Key="HubBorderBrush" Color="{StaticResource HubBorderColor}"/>
    <SolidColorBrush x:Key="HubTextPrimaryBrush" Color="{StaticResource HubTextPrimaryColor}"/>
    <SolidColorBrush x:Key="HubTextMutedBrush" Color="{StaticResource HubTextMutedColor}"/>
    <SolidColorBrush x:Key="HubAccentBrush" Color="{StaticResource HubAccentColor}"/>
    <SolidColorBrush x:Key="HubAccentHoverBrush" Color="{StaticResource HubAccentHoverColor}"/>
    <SolidColorBrush x:Key="HubPrimaryForegroundBrush" Color="{StaticResource HubPrimaryForegroundColor}"/>
    <SolidColorBrush x:Key="HubMetricBgBrush" Color="{StaticResource HubMetricBgColor}"/>
    <SolidColorBrush x:Key="HubInputBgBrush" Color="{StaticResource HubInputBgColor}"/>
    <SolidColorBrush x:Key="HubNowPlayingBrush" Color="{StaticResource HubNowPlayingColor}"/>
    <SolidColorBrush x:Key="HubHoverBrush" Color="{StaticResource HubHoverColor}"/>
    <SolidColorBrush x:Key="HubSelectedBrush" Color="{StaticResource HubSelectedColor}"/>
    <SolidColorBrush x:Key="HubSliderTrackBrush" Color="{StaticResource HubSliderTrackColor}"/>
    <SolidColorBrush x:Key="HubSliderFillBrush" Color="{StaticResource HubSliderFillColor}"/>
    <SolidColorBrush x:Key="HubSliderThumbBrush" Color="{StaticResource HubSliderThumbColor}"/>
    <SolidColorBrush x:Key="HubSurfaceRaisedBrush" Color="{StaticResource HubMetricBgColor}"/>

    <FontFamily x:Key="HubFont">Segoe UI Variable Text, Segoe UI, sans-serif</FontFamily>

    <Style x:Key="HubCardBorder" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource HubCardBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="CornerRadius" Value="12"/>
    </Style>

    <Style x:Key="HubSectionTitle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource HubFont}"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
    </Style>

    <Style x:Key="HubBodyText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource HubFont}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="#E0E0E0"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="HubHintText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource HubFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource HubTextMutedBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="HubSettingLabel" TargetType="TextBlock" BasedOn="{StaticResource HubBodyText}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextWrapping" Value="NoWrap"/>
    </Style>

    <Style x:Key="HubSimpleCheckBox" TargetType="CheckBox">
        <Setter Property="Margin" Value="0,0,0,8"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource HubFont}"/>
    </Style>

    <Style x:Key="HubToolbarButton" TargetType="Button">
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="Background" Value="{DynamicResource HubMetricBgBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource HubFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="20" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#3E3E3E"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#4A4A4A"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HubToolbarToggleButton" TargetType="ToggleButton">
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Margin" Value="0,0,6,0"/>
        <Setter Property="Background" Value="{DynamicResource HubMetricBgBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource HubFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="20" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HubAccentBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#3E3E3E"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HubAccentBrush}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HubPrimaryButton" TargetType="Button" BasedOn="{StaticResource HubToolbarButton}">
        <Setter Property="Background" Value="{DynamicResource HubAccentBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource HubAccentBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{DynamicResource HubAccentBrush}"
                            BorderThickness="0"
                            CornerRadius="20" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          TextElement.Foreground="White"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HubAccentHoverBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HubTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource HubInputBgBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="CaretBrush" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontFamily" Value="{StaticResource HubFont}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontFamily" Value="{StaticResource HubFont}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Padding" Value="10,10"/>
        <Setter Property="Margin" Value="0,1"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}" CornerRadius="8"
                            HorizontalAlignment="Stretch">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HubHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HubSelectedBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Spotify-like thin track + soft fill + small circular thumb -->
    <Style x:Key="HubSliderRepeatTransparent" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HubSliderDecrease" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <!-- Keep fill the same thickness as the track (not the thumb height) -->
                    <Border Background="{DynamicResource HubSliderFillBrush}"
                            CornerRadius="2"
                            Height="4"
                            VerticalAlignment="Center"
                            Margin="0,0,-6,0"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HubSliderThumb" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Width" Value="12"/>
        <Setter Property="Height" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Grid Background="Transparent" Width="12" Height="12">
                        <Ellipse x:Name="Ring" Width="12" Height="12"
                                 Fill="{DynamicResource HubSliderThumbBrush}"
                                 VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Ring" Property="Width" Value="14"/>
                            <Setter TargetName="Ring" Property="Height" Value="14"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="Ring" Property="Width" Value="14"/>
                            <Setter TargetName="Ring" Property="Height" Value="14"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Thumb invisible until slider hover / drag (Spotify volume style) -->
    <Style x:Key="HubSliderThumbAuto" TargetType="Thumb" BasedOn="{StaticResource HubSliderThumb}">
        <Setter Property="Opacity" Value="0"/>
    </Style>

    <ControlTemplate x:Key="HubSliderHorizontal" TargetType="Slider">
        <Grid VerticalAlignment="Center" Height="20" Background="Transparent">
            <Border Height="4" CornerRadius="2" Background="{DynamicResource HubSliderTrackBrush}"
                    VerticalAlignment="Center"/>
            <Track x:Name="PART_Track" VerticalAlignment="Center">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource HubSliderDecrease}" Command="Slider.DecreaseLarge"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource HubSliderThumb}"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource HubSliderRepeatTransparent}" Command="Slider.IncreaseLarge"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="HubSliderHorizontalAutoThumb" TargetType="Slider">
        <Grid x:Name="Root" VerticalAlignment="Center" Height="20" Background="Transparent">
            <Border x:Name="TrackBg" Height="4" CornerRadius="2" Background="{DynamicResource HubSliderTrackBrush}"
                    VerticalAlignment="Center"/>
            <Track x:Name="PART_Track" VerticalAlignment="Center">
                <Track.DecreaseRepeatButton>
                    <RepeatButton x:Name="DecreaseButton" Style="{StaticResource HubSliderDecrease}" Command="Slider.DecreaseLarge"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb x:Name="Thumb" Style="{StaticResource HubSliderThumbAuto}"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource HubSliderRepeatTransparent}" Command="Slider.IncreaseLarge"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Thumb" Property="Opacity" Value="1"/>
            </Trigger>
            <Trigger SourceName="Thumb" Property="IsDragging" Value="True">
                <Setter TargetName="Thumb" Property="Opacity" Value="1"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="HubSliderVertical" TargetType="Slider">
        <Grid HorizontalAlignment="Center" Width="20">
            <Border Width="4" CornerRadius="2" Background="{DynamicResource HubSliderTrackBrush}"
                    HorizontalAlignment="Center"/>
            <Track x:Name="PART_Track" HorizontalAlignment="Center" Orientation="Vertical">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource HubSliderDecrease}" Command="Slider.DecreaseLarge"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource HubSliderThumb}"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource HubSliderRepeatTransparent}" Command="Slider.IncreaseLarge"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style TargetType="Slider">
        <Setter Property="Focusable" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource HubSliderFillBrush}"/>
        <Setter Property="Template" Value="{StaticResource HubSliderHorizontal}"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Template" Value="{StaticResource HubSliderVertical}"/>
                <Setter Property="Width" Value="20"/>
                <Setter Property="MinWidth" Value="20"/>
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="20"/>
                <Setter Property="MinHeight" Value="20"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="HubVolumeSlider" TargetType="Slider" BasedOn="{StaticResource {x:Type Slider}}">
        <Setter Property="Template" Value="{StaticResource HubSliderHorizontalAutoThumb}"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="MinHeight" Value="20"/>
    </Style>

    <!-- Spotify-style carousel page button -->
    <Style x:Key="HubCarouselNavButton" TargetType="Button">
        <Setter Property="Width" Value="34"/>
        <Setter Property="Height" Value="34"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="#CC242424" CornerRadius="17"
                            Width="34" Height="34" BorderBrush="#33FFFFFF" BorderThickness="1">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#E03A3A3A"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FF1A1A1A"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HubTreeExpander" TargetType="ToggleButton">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Width" Value="16"/>
        <Setter Property="Height" Value="16"/>
        <Setter Property="Margin" Value="0,0,4,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="Transparent">
                        <Path x:Name="Arrow" Data="M 0,0 L 4,4 L 0,8 Z" Fill="{DynamicResource HubTextMutedBrush}"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Arrow" Property="Data" Value="M 0,0 L 8,0 L 4,4 Z"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TreeView">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource HubFont}"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <Style TargetType="TreeViewItem">
        <Setter Property="Padding" Value="6,6"/>
        <Setter Property="Margin" Value="0,1"/>
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeViewItem">
                    <StackPanel>
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ToggleButton x:Name="Expander" ClickMode="Press"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                              Style="{StaticResource HubTreeExpander}"/>
                                <ContentPresenter Grid.Column="1" x:Name="PART_Header"
                                                  ContentSource="Header"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ItemsPresenter x:Name="ItemsHost" Margin="16,0,0,0"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HubHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HubSelectedBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsSelectionActive" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HubSelectedBrush}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HubComboBox" TargetType="ComboBox">
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="Background" Value="{DynamicResource HubMetricBgBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FontFamily" Value="{StaticResource HubFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                      Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Bd" Background="{DynamicResource HubMetricBgBrush}"
                                            CornerRadius="14" Padding="8,2,6,2"
                                            TextElement.Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="14"/>
                                            </Grid.ColumnDefinitions>
                                            <ContentPresenter Grid.Column="0" Margin="2,0,4,0"
                                                              VerticalAlignment="Center" HorizontalAlignment="Left"
                                                              Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                              ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                              ContentStringFormat="{Binding SelectionBoxItemStringFormat, RelativeSource={RelativeSource AncestorType=ComboBox}}"/>
                                            <Path Grid.Column="1" Data="M0,0 L4,4 L8,0 Z" Fill="{DynamicResource HubTextMutedBrush}"
                                                  VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#3E3E3E"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Border Margin="0,4,0,0" Background="{DynamicResource HubCardBrush}"
                                    BorderBrush="{DynamicResource HubBorderBrush}" BorderThickness="1"
                                    CornerRadius="10" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="220">
                                <ScrollViewer Margin="4" VerticalScrollBarVisibility="Auto">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource HubTextPrimaryBrush}"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}" CornerRadius="6">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HubHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HubSelectedBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HubPanelSplitter" TargetType="GridSplitter">
        <Setter Property="Width" Value="6"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="ShowsPreview" Value="False"/>
        <Setter Property="ResizeBehavior" Value="PreviousAndNext"/>
        <Setter Property="Cursor" Value="SizeWE"/>
    </Style>

    <Style x:Key="HubPanelChromeButton" TargetType="Button" BasedOn="{StaticResource HubToolbarButton}">
        <Setter Property="Width" Value="26"/>
        <Setter Property="Height" Value="26"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="FontSize" Value="11"/>
    </Style>

    <!-- Subtle scrollbars for vertical lists -->
    <Style TargetType="ScrollBar">
        <Setter Property="Width" Value="8"/>
        <Setter Property="MinWidth" Value="8"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="8"/>
                <Setter Property="MinHeight" Value="8"/>
                <Setter Property="Width" Value="Auto"/>
                <Setter Property="MinWidth" Value="0"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
