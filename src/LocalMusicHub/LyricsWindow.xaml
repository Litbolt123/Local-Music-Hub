<Window x:Class="LocalMusicHub.LyricsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lyrics"
        Height="560" Width="480"
        MinHeight="360" MinWidth="360"
        Background="{DynamicResource HubBgBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,10">
            <TextBlock x:Name="TrackTitleText" Style="{DynamicResource HubSectionTitle}" FontSize="18" TextWrapping="Wrap"/>
            <TextBlock x:Name="SourceText" Style="{DynamicResource HubHintText}" Margin="0,4,0,0"/>
        </StackPanel>
        <Border Grid.Row="1" Style="{DynamicResource HubCardBorder}" Padding="8">
            <Grid>
                <ScrollViewer x:Name="PlainScroll" VerticalScrollBarVisibility="Auto" Visibility="Visible">
                    <TextBlock x:Name="LyricsText" Style="{DynamicResource HubBodyText}" TextWrapping="Wrap"
                               FontSize="15" LineHeight="24" Margin="6"/>
                </ScrollViewer>
                <ListBox x:Name="SyncedList" Visibility="Collapsed"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         BorderThickness="0" Background="Transparent"
                         FontSize="15">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{DynamicResource HubAccentBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Refresh" Style="{DynamicResource HubToolbarButton}" Padding="14,8" Margin="0,0,8,0"
                    Click="Refresh_OnClick"/>
            <Button Content="Close" Style="{DynamicResource HubPrimaryButton}" Padding="16,8"
                    IsCancel="True" Click="Close_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
